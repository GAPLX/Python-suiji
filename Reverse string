#Reverse string
'''
将字符串反转
text[::-1]
'''

#text[a:b:c]
'''
a表示开始的位置，默认为0 [:b:c]
b表示结束的位置，默认为-1 [a::c]
c表示间隔，默认为1 [a:b]
注意：  当c<0时，向前匹配，所以a的位置要比b靠后
       当c>0时，向后匹配，所以a的位置要比b靠前
'''
text = '1234567'
text[::-1]
>>> '7654321'
text[0:3]
>>> '123'
text[-1:2:-1]
>>> '7654'
text[-6:-1:2]
>>> '246'
