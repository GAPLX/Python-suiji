#直接引用迭代器元素的方法
'''
以前我都是用循环语句，来实现对迭代器中元素的运用
今天学到了另一种迭代元素的利用方法，直接设置变量名来进行引用
同时也解答了以前我一直困惑的如何进行批量变量赋值
'''
#使用循环语句
a = map(int,['1','2','3'])
for i in a:
    print(i)
>>>1
2
3
#直接引用
a,b,c = map(int,['1','2','3'])
a+b+c
>>>6
#变量批量赋值
a,b,c = [1,2,3]
a,b,c = (1,2,3)
a,b,c = '123'
a,b,c = {1:'1',2:'2'}
a,b,c = [(1,2),(2,3),(3,4)]
a
>>> 1 1 '1' 1 (1,2)

