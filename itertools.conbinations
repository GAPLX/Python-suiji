# combinations 全排列
'''
combinations(iterable,r)
iterable 排列组合目标对象
r 排列组合的长度
返回一个迭代器，遍历读取
'''
from itertools import combinations as C
text = [1,2,3,4]
a = (text[start:end] for start,end in C(range(len(text)+1),2))
for i in a :
    print(i)
>>>
